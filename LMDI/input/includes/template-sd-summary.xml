<div xmlns="http://www.w3.org/1999/xhtml">
  <table class="grid">
    <thead>
      <tr>
        {% assign page_filename = page.path | split: '/' | last %}
        {% assign page_prefix = page_filename | slice: 0, 3 %}
        {% if page_prefix == 'en-' %}
        <th>Name</th>
        <th>Description</th>
        {% else %}
        <th>Navn</th>
        <th>Beskrivelse</th>
        {% endif %}
      </tr>
    </thead>
    <tbody>
    {% assign items = site.data.structuredefinitions | where: "kind", "resource" | sort: "name" %}
    {% for item in items %}
      <tr>
        <td><a href="{{ item.path }}">{{ item.name }}</a></td>
        <td>{{ item.description }}</td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>
